machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
    COVERALLS_REPO_TOKEN: ZrkuqOp4oS84p2yBJGV31ezoV9OXvdZcp
    COVERALLS_PARALLEL: true

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

general:
  artifacts:
    - "coverage" # Coverage data folder

test:
  override:
    - yarn test-with-coverage

notify:
  webhooks:
    - url: https://coveralls.io/webhook?repo_token=ZrkuqOp4oS84p2yBJGV31ezoV9OXvdZcp
